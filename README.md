# Tapyrus SPV Implementation which runs on Android device

This repository is in WIP.

## How to run

```
$ git submodule update --init tapyrus-spv
$ cd tapyrus-spv
$ ./spcrits/build-ndk.sh
```

Then run android app in such a way as usual one.